export syscall setColor(color)
export syscall rect(x, y, w, h, color)
export syscall fillRect(x, y, w, h, color)
export syscall cls(color)
export syscall circle(x, y, r, color)
export syscall fillCircle(x, y, r, color)
export syscall line(x1, y1, x2, y2, color)
export syscall camera(x = 0, y = 0)
export syscall pixel(x, y, color)
export syscall getPixel(x, y)
export syscall text(x, y, str, color)
export syscall textMono(x, y, str, color)
export syscall setFont(name)
